{% extends 'base.html.twig' %}

{% block body %}
    <h1>Formato</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ formato.nombre }}</td>
            </tr>
            <tr>
                <th>Icono</th>
                <td>{{ formato.icono }}</td>
            </tr>
            <tr>
                <th>Usastock</th>
                <td>{% if formato.usaStock %}Yes{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ formato.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('formato_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('formato_edit', { 'id': formato.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
