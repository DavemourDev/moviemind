{% extends 'base.html.twig' %}

{% block body %}
    <h1>Genero</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombrees</th>
                <td>{{ genero.nombreEs }}</td>
            </tr>
            <tr>
                <th>Nombreen</th>
                <td>{{ genero.nombreEn }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ genero.descripcion }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ genero.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('crud_genero_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('crud_genero_edit', { 'id': genero.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
