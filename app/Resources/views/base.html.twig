<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Moviemind{% endblock %}</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.0/jquery.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

        <style>
            /*
                ESTILOS DE BASE. MODIFICAR SOLAMENTE AQUÍ
            */
            body { background: #F5F5F5; font-size: 1.5em; font-family: 'Roboto', sans-serif;}
            h1, h2 { line-height: 1.2; margin: 0 0 .5em; }
            h1 { font-size: 36px; }
            h2 { font-size: 21px; margin-bottom: 1em; }
            p { margin: 0 0 1em 0; }
            a { color: #0000F0; }
            a:hover { text-decoration: none; }
            code { background: #F5F5F5; max-width: 100px; padding: 2px 6px; word-wrap: break-word; }
            #wrapper { background: #FFF; margin: 1em auto; max-width: 800px; width: 95%; }
            #container 
            { 
                width:80%; 
                max-width: 1080px;
                min-width:380px;
                margin:auto;
                display:flex;
                flex-direction:column;
            }

            #navigation
            {
                display:flex;
                flex-direction:row;
                flex-wrap: wrap;
                justify-content:space-between;
                background:#333;

            }

            #navigation>.nav-panel
            {
                display:flex;
                flex-direction:row;
                flex-wrap: wrap;
                justify-content:space-around;
                align-content: center;
            }

            #navigation>.nav-panel>a, .link-nav
            {
                color:#fff;
                padding:.25rem;
                text-decoration:none;
            }
            .link-nav{
                background-color:transparent;
                border: none;
            }
            @media (min-width: 768px) {
                #wrapper { width: 80%; margin: 2em auto; }
                #icon-book { display: inline-block; }
                #status a, #next a { display: block; }

                @-webkit-keyframes fade-in { 0% { opacity: 0; } 100% { opacity: 1; } }
                @keyframes fade-in { 0% { opacity: 0; } 100% { opacity: 1; } }
                .sf-toolbar { opacity: 0; -webkit-animation: fade-in 1s .2s forwards; animation: fade-in 1s .2s forwards;}
            }
            .big-title{
                font-family: 'Dosis', sans-serif;
                font-size: 4em;
                margin-bottom: 0.3em;
                text-align: center;
                letter-spacing: 10px;
            }
            #search-block{
                position:absolute;
                top:30%;
                left:35%;
            }
            footer{
                position:absolute;
                bottom:0px;
            }
            .search-input{
                border-radius:2px;
            }

            .search-input:focus{
                box-shadow: 0 0px 0px 0 rgba(0, 0, 0, 0.0), 0 0px 0px 0 rgba(0, 0, 0, 0.0);
                border-color:#ccc;
                outline: none;
            }
            .search-form{
                position:relative;
            }
            .search-button{
                position:absolute;
                top:0px;
                right:0px;
                background-color: #ccc;
            }
            #search-input-home{
                box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.1), 0 6px 20px 0 rgba(0, 0, 0, 0.1);
            }
            #search-input-home:focus{
                box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
                border-color:#ccc;
                outline: none;
            }
            #search-input-results{
                border: 0;
                outline: 0;
                background: transparent;
                border-bottom: 1px solid white;
                font-size:0.7em;
                color:white;
                width: 150px;
            }
            #search-button-results{
                background-color:transparent;
                color:white;
                border:none;
                font-size:0.7em;

            }
            #search-input-results:focus{
                border-bottom: 1px solid #ccc;
            }
            #home-link{
                font-family: 'Dosis', sans-serif;
                letter-spacing:2px;
            }

            .nav-panel>a{
                opacity:1;
            }

            .nav-panel>a:hover{
                opacity:0.7;
            }
            @media (max-width: 800px) {
                #login-text{
                    display:none;
                }
            }
            #home-link:hover{
                font-weight: bold;
            }
            #navigation div{
                padding:10px;
            }
            #login-div{
                transition: background-color 0.2s;
            }
            .result-article{
                padding:20px;   
            }
            .dropbtn {
                background-color: #4CAF50;
                color: white;
                padding: 16px;
                font-size: 16px;
                border: none;
                cursor: pointer;
            }

            .dropdown {
                position: relative;
                display: inline-block;
            }

            .dropdown-content {
                display: none;
                position: absolute;
                background-color: #f9f9f9;
                min-width: 160px;
                box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
                z-index: 1;
            }

            .dropdown-content a {
                color: black;
                padding: 12px 12px;
                text-decoration: none;
                display: block;
            }

            .dropdown-content a:hover {background-color: #f1f1f1}

            .dropdown:hover .dropdown-content {
                display: block;
            }

            .dropdown:hover .dropbtn {
                background-color: #3e8e41;
            }
            .caratula{
                width: 120px;  
            }
            @media (max-width: 576px) {
                .details-results-div, #details-results-div{
                    width: 100%;
                }
            }
            .caratula-info-fig, .caratula-info-img{
                width: 220px;
            }
            .caratula-info-img{
                width:100%;
            }
            .details-results-div{
                padding-top: 20px;
            }
            .paragraph{
                clear:both;
                padding:30px;
            }
            figure{
                padding:0px;
            }
            .caratula-info-img{
                box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);

            }
            @media (max-width: 400px) {
                .nav-panel{
                    width: 100%;
                    text-align: center;
                }
                .link-nav{
                    width:300px;
                }
            }

            .badge {
                background-color: #6394F8;
                border-radius: 10px;
                color: white;
                display: inline-block;
                font-size: 12px;
                line-height: 1;
                padding: 3px 7px;
                text-align: center;
                vertical-align: middle;
                white-space: nowrap;
            }

            .shopping-cart {
                margin: 20px 5px;
                float: right;
                background: white;
                width: 320px;
                position: relative;
                border-radius: 3px;
                padding: 20px;
            }
            .shopping-cart:after {
                bottom: 100%;
                left: 89%;
                border: solid transparent;
                content: " ";
                height: 0;
                width: 0;
                position: absolute;
                pointer-events: none;
                border-bottom-color: white;
                border-width: 8px;
                margin-left: -8px;
            }

            .cart-icon {
                color: #515783;
                font-size: 24px;
                margin-right: 7px;
                float: left;
            }

            .button {
                color:white;
                text-align: center;
                padding: 12px;
                text-decoration: none;
                display: block;
                border-radius: 3px;
                font-size: 16px;
                margin: 25px 0 15px 0;
            }

            .clearfix:after {
                content: "";
                display: table;
                clear: both;
            }
            .shopping-cart .shopping-cart-header {
                border-bottom: 1px solid #E8E8E8;
                padding-bottom: 15px;
            }
            .cart-div{
                padding-right: 25px !important;
            }
            #checkout-link{
                background-color: #d2d2d2;
                color:#333;
            }
            #checkout-link:hover{
                color:#ccc;
                background-color:#333;
            }

            .shopping-cart-header {
                border-bottom: 1px solid #E8E8E8;
                padding-bottom: 15px;
                height: 35px;
            }
            .shopping-cart-header .shopping-cart-total {
                float: right;
            }
            .shopping-cart-items {

                padding-top: 20px;
            }
            .shopping-cart-items li {
                margin-bottom: 18px;
            }

            .shopping-cart-items  img {
                float: left;
                margin-right: 12px;
            }

            .shopping-cart-items  .item-name {
                display: block;
                padding-top: 10px;
                font-size: 16px;
            }

            .shopping-cart-items  .item-price {
                margin-right: 8px;
            }
            .shopping-cart-items{
                list-style: none;
                padding-left: 10px;
            }
            .delete-icon{
                text-decoration: none;
                color:inherit;
            }

        </style>
        <link href="https://fonts.googleapis.com/css?family=Dosis|Roboto" rel="stylesheet">
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>

        <nav id="navigation">
            {% block navigation %}
                {% include 'navigation/main-navigation.html.twig' %}
                {% include 'navigation/barra-busqueda.html.twig' %}               
                {% include 'navigation/no-logged.html.twig'%}
                {% include 'navigation/carrito.html.twig' %}

            {% endblock %}
        </nav>
        <aside id="sidebar">
            {% block aside %}
                {% include 'carrito/cart-content.html.twig' %}
            {% endblock %}
        </aside>
        <div id="container" >

            <main id="main" class="container container-fluid">
                {% block main %}
                {% endblock %}
                {% block body %}
                {% endblock %}
            </main>

            <footer id="footer">
                {% block footer %}
                {% endblock %}
            </footer>
        </div>
        <!--David:
            Pongo aquí el javascript común a todas las páginas.
            Si en una página nos da por añadir scripts usando el bloque script nos machacará lo que haya en él en esta plantilla.
        -->
        <script src="{{asset('js/script.js')}}"></script>        
        {% block javascripts %}
        {% endblock %}

    </body>
</html>
